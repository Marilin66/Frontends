import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Couleurs principales
  static const Color primary = Color(0xFF1565C0);
  static const Color primaryLight = Color(0xFF42A5F5);
  static const Color primaryDark = Color(0xFF0D47A1);

  // Couleurs secondaires
  static const Color secondary = Color(0xFF26A69A);
  static const Color secondaryLight = Color(0xFF80CBC4);
  static const Color secondaryDark = Color(0xFF00796B);

  // Couleurs de fond
  static const Color background = Color(0xFFF5F7FA);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color card = Color(0xFFFFFFFF);

  // Couleurs de texte
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textHint = Color(0xFFBDBDBD);

  // Couleurs d'état
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFF9800);
  static const Color error = Color(0xFFE53935);
  static const Color info = Color(0xFF2196F3);

  // Couleurs spécifiques aux rôles
  static const Color patient = Color(0xFF1565C0);
  static const Color medecin = Color(0xFF2E7D32);
  static const Color adminHopital = Color(0xFFE65100);
  static const Color superAdmin = Color(0xFF6A1B9A);
  static const Color laborantin = Color(0xFF00838F);
  static const Color divider = Color(0xFFEEEEEE);
  
  // Couleurs Elite
  static const Color slate950 = Color(0xFF0F172A); // Très sombre pour designs premium
}
